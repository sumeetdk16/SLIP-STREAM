{
  "name": "slipstream",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slipstream — a Chrome extension that carries your conversation context across ChatGPT, Claude, Gemini, Copilot and Grok.",
  "scripts": {
    "test": "node --test test/",
    "check": "node scripts/check.js",
    "verify": "npm run check && npm test",
    "zip": "node scripts/zip.js"
  },
  "devDependencies": {
    "jsdom": "^28.0.0"
  },
  "dependencies": {
    "npm": "^12.0.2",
    "run": "^2.1.4",
    "verify": "^0.1.2"
  }
}
